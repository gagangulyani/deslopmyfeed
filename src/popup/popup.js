import { DEFAULT_SETTINGS, loadSettings, saveSettings } from '../storage/settings.js';

/** Render the panel described in spec §12 and wire every control to storage. */
async function render() {
  throw new Error('not implemented');
}

render();
